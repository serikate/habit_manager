# 習慣レビュー機能 実装手順書

## 実装概要

### 対象ファイル（新規作成）
- `components/habits/HabitReviewModal.tsx` - レビューモーダルコンポーネント
- `stores/habitReviewStore.ts` - レビュー状態管理
- `utils/habitReviewCalculator.ts` - スコア計算ロジック

### 対象ファイル（修正）
- `app/habits/page.tsx` - レビューボタン追加
- Supabaseマイグレーション - テーブル作成

---

## Step 1: データベーステーブル作成

### 1.1 マイグレーションファイル作成

Supabase MCPを使用して以下のマイグレーションを適用：

```sql
-- habit_reviewsテーブル作成
CREATE TABLE habit_reviews (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  habit_id UUID NOT NULL REFERENCES habits(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,

  q1_score INTEGER NOT NULL CHECK (q1_score BETWEEN 1 AND 5),
  q2_score INTEGER NOT NULL CHECK (q2_score BETWEEN 1 AND 5),
  q3_score INTEGER NOT NULL CHECK (q3_score BETWEEN 1 AND 5),
  q4_score INTEGER NOT NULL CHECK (q4_score BETWEEN 1 AND 5),
  q5_score INTEGER NOT NULL CHECK (q5_score BETWEEN 1 AND 5),

  total_score DECIMAL(4,1) NOT NULL,
  percentage DECIMAL(5,2) NOT NULL,
  judgment TEXT NOT NULL CHECK (judgment IN ('continue', 'adjust', 'reconsider', 'stop')),

  snapshot JSONB NOT NULL,

  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- インデックス
CREATE INDEX idx_habit_reviews_habit_id ON habit_reviews(habit_id);
CREATE INDEX idx_habit_reviews_user_id ON habit_reviews(user_id);
CREATE INDEX idx_habit_reviews_created_at ON habit_reviews(created_at DESC);

-- RLS
ALTER TABLE habit_reviews ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own reviews"
  ON habit_reviews FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can create own reviews"
  ON habit_reviews FOR INSERT
  WITH CHECK (auth.uid() = user_id);
```

---

## Step 2: スコア計算ユーティリティ作成

### 2.1 `utils/habitReviewCalculator.ts`

```typescript
// 質問の重み設定
export const QUESTION_WEIGHTS = {
  q1: 1.5,  // 目標貢献度
  q2: 1.2,  // 目的意識
  q3: 1.0,  // 時間適切さ
  q4: 1.0,  // 生活との調和
  q5: 1.3,  // モチベーション
} as const

// 質問ラベル
export const QUESTION_LABELS = {
  q1: 'この習慣は短期目標の達成に貢献していますか？',
  q2: 'この習慣を目的意識を持って実行できていますか？',
  q3: 'この習慣にかけている時間は適切ですか？',
  q4: 'この習慣は生活リズムに無理なく組み込めていますか？',
  q5: 'この習慣へのモチベーションは維持できていますか？',
} as const

// 回答選択肢
export const ANSWER_OPTIONS = [
  { value: 5, label: '強くそう思う' },
  { value: 4, label: 'そう思う' },
  { value: 3, label: 'どちらでもない' },
  { value: 2, label: 'あまりそう思わない' },
  { value: 1, label: '全くそう思わない' },
] as const

// 判定タイプ
export type JudgmentType = 'continue' | 'adjust' | 'reconsider' | 'stop'

// 判定結果
export interface ReviewResult {
  totalScore: number
  maxScore: number
  percentage: number
  judgment: JudgmentType
  message: string
  color: string
  icon: string
}

// 満点計算
export const MAX_SCORE = 5 * (
  QUESTION_WEIGHTS.q1 +
  QUESTION_WEIGHTS.q2 +
  QUESTION_WEIGHTS.q3 +
  QUESTION_WEIGHTS.q4 +
  QUESTION_WEIGHTS.q5
) // = 30

// スコア計算
export function calculateReviewScore(answers: {
  q1: number
  q2: number
  q3: number
  q4: number
  q5: number
}): ReviewResult {
  const totalScore =
    answers.q1 * QUESTION_WEIGHTS.q1 +
    answers.q2 * QUESTION_WEIGHTS.q2 +
    answers.q3 * QUESTION_WEIGHTS.q3 +
    answers.q4 * QUESTION_WEIGHTS.q4 +
    answers.q5 * QUESTION_WEIGHTS.q5

  const percentage = (totalScore / MAX_SCORE) * 100

  let judgment: JudgmentType
  let message: string
  let color: string
  let icon: string

  if (percentage >= 80) {
    judgment = 'continue'
    message = 'この習慣は目標達成に貢献しています'
    color = 'green'
    icon = '✅'
  } else if (percentage >= 60) {
    judgment = 'adjust'
    message = '頻度や方法を見直してみましょう'
    color = 'yellow'
    icon = '🔄'
  } else if (percentage >= 40) {
    judgment = 'reconsider'
    message = '目標との関連を再確認しましょう'
    color = 'orange'
    icon = '⚠️'
  } else {
    judgment = 'stop'
    message = '別の習慣への切り替えを検討しましょう'
    color = 'red'
    icon = '❌'
  }

  return {
    totalScore: Math.round(totalScore * 10) / 10,
    maxScore: MAX_SCORE,
    percentage: Math.round(percentage * 10) / 10,
    judgment,
    message,
    color,
    icon,
  }
}
```

---

## Step 3: Zustand Store作成

### 3.1 `stores/habitReviewStore.ts`

```typescript
import { create } from 'zustand'
import { supabase } from '@/lib/supabase'
import { calculateReviewScore, type ReviewResult } from '@/utils/habitReviewCalculator'

interface HabitReviewState {
  // 状態
  isModalOpen: boolean
  currentHabitId: string | null
  answers: {
    q1: number | null
    q2: number | null
    q3: number | null
    q4: number | null
    q5: number | null
  }
  result: ReviewResult | null
  isSubmitting: boolean
  showResult: boolean

  // アクション
  openModal: (habitId: string) => void
  closeModal: () => void
  setAnswer: (question: 'q1' | 'q2' | 'q3' | 'q4' | 'q5', value: number) => void
  calculateResult: () => void
  submitReview: (habitSnapshot: object) => Promise<void>
  resetState: () => void
}

export const useHabitReviewStore = create<HabitReviewState>((set, get) => ({
  isModalOpen: false,
  currentHabitId: null,
  answers: { q1: null, q2: null, q3: null, q4: null, q5: null },
  result: null,
  isSubmitting: false,
  showResult: false,

  openModal: (habitId) => {
    set({
      isModalOpen: true,
      currentHabitId: habitId,
      answers: { q1: null, q2: null, q3: null, q4: null, q5: null },
      result: null,
      showResult: false,
    })
  },

  closeModal: () => {
    set({ isModalOpen: false })
    // 少し遅らせてリセット
    setTimeout(() => get().resetState(), 300)
  },

  setAnswer: (question, value) => {
    set((state) => ({
      answers: { ...state.answers, [question]: value }
    }))
  },

  calculateResult: () => {
    const { answers } = get()
    if (
      answers.q1 !== null &&
      answers.q2 !== null &&
      answers.q3 !== null &&
      answers.q4 !== null &&
      answers.q5 !== null
    ) {
      const result = calculateReviewScore({
        q1: answers.q1,
        q2: answers.q2,
        q3: answers.q3,
        q4: answers.q4,
        q5: answers.q5,
      })
      set({ result, showResult: true })
    }
  },

  submitReview: async (habitSnapshot) => {
    const { currentHabitId, answers, result } = get()
    if (!currentHabitId || !result) return

    set({ isSubmitting: true })

    try {
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) throw new Error('User not found')

      const { error } = await supabase.from('habit_reviews').insert({
        habit_id: currentHabitId,
        user_id: user.id,
        q1_score: answers.q1,
        q2_score: answers.q2,
        q3_score: answers.q3,
        q4_score: answers.q4,
        q5_score: answers.q5,
        total_score: result.totalScore,
        percentage: result.percentage,
        judgment: result.judgment,
        snapshot: habitSnapshot,
      })

      if (error) throw error
    } catch (error) {
      console.error('Failed to submit review:', error)
      throw error
    } finally {
      set({ isSubmitting: false })
    }
  },

  resetState: () => {
    set({
      currentHabitId: null,
      answers: { q1: null, q2: null, q3: null, q4: null, q5: null },
      result: null,
      showResult: false,
    })
  },
}))
```

---

## Step 4: レビューモーダルコンポーネント作成

### 4.1 `components/habits/HabitReviewModal.tsx`

主要な実装ポイント：
- モーダル形式で表示
- 5つの質問をラジオボタンで回答
- 全問回答後に「結果を見る」ボタン有効化
- 結果画面でスコアとグラフを表示
- 保存ボタンでDBに記録

---

## Step 5: 習慣ページに統合

### 5.1 `app/habits/page.tsx` の修正

- 各習慣カードに「レビュー」ボタンを追加
- HabitReviewModalをページに配置
- レビューボタンクリック時にモーダルを開く

---

## Step 6: テストと動作確認

### 6.1 確認項目

- [ ] マイグレーションが正常に適用される
- [ ] モーダルが正常に開閉する
- [ ] 5つの質問に回答できる
- [ ] 全問回答後に結果が表示される
- [ ] スコア計算が正しい
- [ ] 判定結果が正しく表示される
- [ ] DBにレビュー結果が保存される
- [ ] RLSが正しく動作する

### 6.2 テストケース

| ケース | 回答 (Q1-Q5) | 期待スコア | 期待判定 |
|--------|-------------|-----------|---------|
| 最高評価 | 5,5,5,5,5 | 30.0 (100%) | 継続 |
| 高評価 | 4,4,4,4,4 | 24.0 (80%) | 継続 |
| 中評価 | 3,3,3,3,3 | 18.0 (60%) | 調整 |
| 低評価 | 2,2,2,2,2 | 12.0 (40%) | 要検討 |
| 最低評価 | 1,1,1,1,1 | 6.0 (20%) | 中止検討 |

---

## 実装順序まとめ

1. **Step 1**: DBマイグレーション適用
2. **Step 2**: スコア計算ユーティリティ作成
3. **Step 3**: Zustand Store作成
4. **Step 4**: レビューモーダルコンポーネント作成
5. **Step 5**: 習慣ページに統合
6. **Step 6**: テスト・動作確認
7. **ビルド確認**: `npm run build`
